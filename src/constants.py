"""Centralized constants for PAFS."""

# Authentication timeout in seconds
TIMEOUT_SECONDS = 300

# Microsoft login page hosts for detecting authentication redirects
LOGIN_HOSTS = [
    "login.microsoftonline.com",
    "login.microsoft.com",
    "login.live.com",
    "account.microsoft.com",
]
